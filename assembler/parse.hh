#include <string>
#include "ast.hh"
#include "tokenizer.hh"

#pragma once

#define EOF '\0'

namespace A216
{
    AST parse(const std::vector<Token> &tokens)
    {
    }

    AST recognize_rule(size_t &index)
    {
    }
}