use lolly as 10

structure My_struct:
    name: word,
    last_name: word,
    age: byte

section .text
_code:
    var x: byte = 10    ; create a variable saved into the stack
    reserve lolly, "Hello World!", hi

; let's see if comments work properly